{
  "name": "hugo",
  "version": "1.0.0",
  "description": "Bilingual Hugo resume site with reusable resume variants, RTL support, and print-friendly output.",
  "main": "index.js",
  "scripts": {
    "build:css": "sass assets/scss/main.scss assets/css/main.css --no-source-map",
    "dev": "node scripts/run-hugo.cjs dev",
    "dev:mobile": "node scripts/mobile-preview.cjs",
    "build": "node scripts/run-hugo.cjs build",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "lint:md": "markdownlint-cli2 \"README.md\"",
    "lint:css": "stylelint \"assets/**/*.scss\" --allow-empty-input",
    "lint": "npm run lint:md && npm.cmd run lint:css",
    "validate": "npm run format:check && npm run lint && npm run build",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.md": [
      "markdownlint-cli2",
      "prettier --write"
    ],
    "*.{json,jsonc,yml,yaml,css,scss,js,cjs,mjs}": [
      "prettier --write"
    ],
    "assets/**/*.scss": [
      "stylelint --fix"
    ]
  },
  "keywords": [],
  "author": "Anan Saleh",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "browser-sync": "^3.0.4",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "markdownlint-cli2": "^0.22.0",
    "prettier": "^3.8.1",
    "sass": "^1.98.0",
    "stylelint": "^17.6.0",
    "stylelint-config-standard": "^40.0.0"
  }
}
